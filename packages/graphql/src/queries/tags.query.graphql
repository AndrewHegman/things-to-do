query TagsByCategory($categoryId: String!) {
  tagsByCategory(categoryId: $categoryId) {
    id
    name
    category {
      id
      name
    }
  }
}

query GetTags {
  tags {
    id
    name
    category {
      id
      name
    }
  }
}
