query GetCategory($categoryId: String!) {
  category(categoryId: $categoryId) {
    id
    name
    tags {
      id
      name
    }
    things {
      id
      name
      description
      tags {
        id
        name
      }
    }
  }
}
