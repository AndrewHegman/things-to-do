mutation CreateThing($name: String!, $description: String!, $tags: [String!]!, $category: String!) {
  createThing(name: $name, description: $description, tags: $tags, category: $category) {
    id
    name
    description
    category {
      id
      name
    }
    tags {
      id
      name
    }
  }
}
