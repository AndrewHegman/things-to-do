import { createSelector } from "reselect";
import { RootState } from "../store";

export const selectors = {
  // getCategoryFromKey: (state: RootState, key: string) => state.common.categories.find((category) => category.key === key),
};
