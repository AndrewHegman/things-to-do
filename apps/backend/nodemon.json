{
  "ignore": [
    "*.test.ts",
    "fixtures/*"
  ],
  "ext": "ts",
  "watch": [
    "src/"
  ],
  "exec": "yarn build && node dist/index.js",
  "events": {
    "restart": "kill-port 4000",
    "crash": "kill-port 4000"
  }
}